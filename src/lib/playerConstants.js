// Shared playback constants (kept separate from PlayerProvider so the provider
// module only exports its component/hook and stays Fast-Refresh friendly).

export const SPEEDS = [0.75, 1, 1.25, 1.5, 2, 2.5, 3];
export const SKIP_SECONDS = 15;
